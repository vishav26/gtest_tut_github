/**
 * @file	cube.hpp
 * @brief	math implementations
 */

#include <cmath>
/**	Power Class
 *
 */
class power
{
public:
	/**
	 * A Constructor
	 * A more elaborate description of the constructor.
	 */
	power(int num);
	/**
	 * computes the square of the function
	 * @param	b	The number whose square has to be computed
	 * @return	int
	 */
	int square(int b);

private:
	int a;
};
double cubic(double d);
